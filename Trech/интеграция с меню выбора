function startScenario(scenarioId) {
    const scenario = scenarios[scenarioId];
    
    // Сохраняем настройки
    scenarioSettings = {
        ...scenario,
        useModifiedMythos: gameConfig.useModifiedMythos
    };
    
    initGame(scenarioSettings);
}